<header>
  <% if user_signed_in? %>
    <%= link_to t('views.common.title_index', name: Book.model_name.human), books_path %>
    <%= link_to t('views.common.title_index', name: User.model_name.human), users_path %>
    <%= link_to t('users.show.title'), user_path(current_user) %>
    <%= link_to t('views.common.logout'), destroy_user_session_path, data: { turbo_method: :delete } %>
  <% end %>
</header>
